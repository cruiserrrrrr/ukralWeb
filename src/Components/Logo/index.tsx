import React from 'react';
import styles from './index.module.scss';
import Icon from '../Icon';

const Logo = () => {
    return (
        <div className={styles.logo}>
            <p className={styles.logo_text}>ukral</p>
        </div>
    )
}

export default Logo;